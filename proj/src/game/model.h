#pragma once

#include <lcom/lcf.h>
#include <stdio.h>

#include "images.h"

void (addMenuEntry)();
void (backMenuEntry)();
int (getCurrentEntryImg)();

int (getPlayerX)();
int (getPlayerY)();
void (playerUp)();
void (playerDown)();
